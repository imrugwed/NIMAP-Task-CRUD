spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nimap_task_crud
    username: root
    password: Pass@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081